/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ["d1mauojdrnebgs.cloudfront.net"],
  },
  turbopack: {},
};

export default nextConfig;
